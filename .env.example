VITE_API_PROXY_TARGET=http://localhost:8080
# Apenas em produção (GH Pages / deploy): aponta para a API publicada.
# Em dev, o Vite faz proxy de /api → backend local automaticamente.
# VITE_API_BASE_URL=https://whodo-ledger-api.fly.dev/api
